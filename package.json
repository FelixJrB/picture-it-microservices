{
  "name": "resource-service",
  "version": "1.0.0",
  "description": "The Resource service is responsible for handling the resources in the overall application, this includes image URLs, description and titles. However the images are stored by the Image service and not by the Resource service.",
  "license": "MIT",
  "author": "Felix Berglund",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.2.3"
  }
}
